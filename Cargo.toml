[package]
name = "vaping-fping"
version = "0.1.0"
edition = "2025"

[lib]
name = "vaping_fping"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[build-dependencies]
pyo3-build-config = "0.22"